source ~/.bashrc
eval "$(pyenv init -)"
source "$HOME/.cargo/env"


export EDITOR=code
export BROWSER=/mnt/c/Progra~1/Google/Chrome/Application/chrome.exe
export PYTEST_ADDOPTS=--showlocals

alias url="${BROWSER}"
